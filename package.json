{
  "name": "daniels-portfolio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "my-portfolio"
  ],
  "scripts": {
    "install": "npm install --prefix my-portfolio",
    "build": "npm run build --prefix my-portfolio",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d my-portfolio/build"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
